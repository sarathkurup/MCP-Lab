.vscode/**
.git*
.github/**
src/**
tests/**
out/**
node_modules/**
esbuild.js
tsconfig.json
**/*.map

# Source artwork: far too large to ship. Set an `icon` in package.json with a
# 128x128 PNG if you want one on the extension.
logo*.png

# Retina source for the marketplace icon; nothing references it at runtime.
media/icon@2x.png

# Development-only helpers.
install.cmd
package-lock.json
